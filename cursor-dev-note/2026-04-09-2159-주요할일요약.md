# 주요 할 일 요약 (2026-04-09 21:59)

다음은 프로젝트 문서·현재 코드 기준으로 **앞으로 이어서 하면 좋은 작업**을 정리한 것입니다. 우선순위는 팀 상황에 맞게 조정하면 됩니다.

---

## 단기 (운영·완성도)

1. **Slack 앱 설정 점검**  
   - Slash Commands 4종(`/todo`, `/list`, `/status`, `/help`) 요청 URL·Socket Mode와 일치 여부  
   - 필요 시 Bot Events: `reaction_added` 등 설계에 맞게 추가 + 스코프 정리 (`reminders:read` 등 미사용이면 검토)

2. **시크릿 관리**  
   - 채팅/스크린샷에 노출된 토큰이 있으면 Slack에서 회전(재발급) 후 `.env`만 갱신

3. **배포**  
   - 프로덕션에서는 HTTP + 고정 URL 또는 호스팅 환경에 맞는 Socket/HTTP 선택, `PORT`·환경변수 문서화

---

## 중기 (설계·do 문서 반영)

4. **SQLite + `src/db/init.js`**  
   - 스키마(`tasks`, `users`, `events_log`) 생성, `DB_PATH`(`data/`) 실제 사용

5. **`dataService`를 DB 기반으로**  
   - 인메모리 `taskStore` 대체 또는 이중화 정책 결정

6. **이벤트**  
   - `reaction_added` 등으로 작업 상태 변경 (설계 문서 Phase 5)

7. **입력 검증·에러 메시지**  
   - 설계의 에러 타입별 사용자 메시지 정리

---

## 장기·품질

8. **로깅** (`winston` / `pino` 등) — 설계 10절  
9. **테스트** — `jest`, 핸들러 단위·통합 테스트 (`tests/`)  
10. **배포 문서** — `deploy.md`, Docker/Procfile 등 필요 시

---

## 메모

- 로컬 개발은 **Socket Mode + `SLACK_APP_TOKEN`** 조합이 단순함.  
- `cursor-dev-note`의 작업 요약 파일과 함께 버전 관리할지 팀 규칙만 정하면 됨 (개인 메모만 두려면 `.gitignore`에 폴더 추가 검토).

---

## 진행 기록 (추가 구현)

- **SQLite**: `src/db/init.js` 스키마 + 기존 DB용 컬럼 마이그레이션, `src/db/tasksRepo.js`, `dataService` DB 연동, 인메모리 `taskStore` 제거.
- **로깅**: `pino` + `src/logger.js`, 일부 핸들러에서 사용자 메시지 정리.
- **이벤트**: `reaction_added` 로그 + `white_check_mark` 시 `slack_message_ts`가 일치하는 작업만 `done` 처리(메시지 연동 시).
- **테스트**: `tests/validators.test.js` (Jest).
